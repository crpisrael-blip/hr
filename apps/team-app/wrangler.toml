name = "hr-team-app"
compatibility_date = "2025-09-01"

# אפליקציית SPA: כל נתיב לא-קיים חוזר ל-index.html כדי שה-router יטפל בו.
[assets]
directory = "./dist"
not_found_handling = "single-page-application"
