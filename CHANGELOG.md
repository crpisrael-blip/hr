# יומן שינויים

הפורמט מבוסס על [Keep a Changelog](https://keepachangelog.com/he/1.1.0/).
מספור הגרסאות עוקב אחרי גרסת האפיון שב-[`docs/SPEC.md`](docs/SPEC.md).

> **הערה על נקודת הפתיחה.** היומן הזה נפתח ב-11.9.2026, אחרי שהמערכת כבר
> בייצור ואחרי 51 קומיטים. הרשומות שלפני התאריך הזה משוחזרות מיומן השינויים
> של האפיון ומהיסטוריית ה-Git ואינן מתויגות ב-Git — **אין tags בריפו כרגע**.
> התיוג מתחיל מהגרסה הבאה; ראו `docs/DEPLOY.md` סעיף 9.

## [לא משוחרר]

### נוסף
- **מחיקה מלאה של משתמש** (`0036_delete_user.sql`, פונקציית הקצה `delete-employee`):
  מכרטיס העובד ניתן למחוק משתמש לגמרי — גם חשבון ההתחברות (`auth.users`) וגם
  כרטיס העובד. ההרשאה שמורה ל**מנהל על בלבד** כברירת מחדל (מטריצת
  `users_permissions/delete`), וניתנת להרחבה למנהלת/מגייס דרך מסך ההרשאות.
  אם לעובד יש רשומות עסקיות מקושרות (השמות/חיובים/משימות) — ההיסטוריה נשמרת:
  חשבון הכניסה מוסר והכרטיס מסומן "הסתיים". בדיקות: `supabase/tests/delete_user.sql`.
- **משתתפים ואישור השתתפות ביומן** (`0035_calendar_participants.sql`): כל עובד
  יכול לזמן כל עובד אחר לאירוע; המוזמן רואה את האירוע ביומן שלו ומאשר/דוחה
  השתתפות. פרטי אירוע נפתחים בלחיצה מתוך היומן. RLS ייעודי (עוזרי SECURITY
  DEFINER למניעת רקורסיה). בדיקות: הורחב `supabase/tests/calendar.sql`.
- **עריכת משימה** (`pages/TaskDetail.tsx`): עריכת כותרת, תיאור, עדיפות, יעד
  וכלל השלמה מתוך כרטיס המשימה.
- **ניווט בין חודשים בתזרים** (`pages/Settlements.tsx`): מעבר חודש קדימה/אחורה,
  תצוגת חודש בודד מול "כל החודשים", וקפיצה מטבלת החודשים לפירוט החודש.
- **לוח שנה** (`0034_calendar.sql`, `pages/Calendar.tsx`): תצוגה חודשית המשלבת
  איסוף אוטומטי של תאריכים קיימים — ראיונות, משימות עם מועד, תחילת עבודה, סיום
  אחריות ומועדי תשלום — יחד עם אירועים ידניים חדשים (`app.calendar_events`).
  נראוּת: כל מגייס רואה ועורך את שלו, המנהלת רואה ועורכת הכול (RLS ייעודי). מחליף
  את מסך ה"בקרוב" בנתיב `/calendar`. בדיקות: `supabase/tests/calendar.sql`.
- **תזרים, חיובים ותקבולים** (`0033_cashflow_billing.sql`, מסך הכספים): תצוגת
  `finance.cashflow` מציגה את הכנסת ההשמה כ"צפוי" מרגע יצירתה (לפי העמלה הצפויה
  ולוח התשלומים בהסכם), עוברת להישען על חיובים אמיתיים עם אישור ההשמה, ומוציאה
  מהתזרים השמה שנכשלה. `app.confirm_invoice_receipt` מאשר קבלת תשלום (מלא/חלקי)
  ומעדכן את מצב החיוב; `app.issue_invoice` מסמן הפקת חיוב. מסך "כספים" קיבל
  לשוניות "תזרים" ו"חיובים ותקבולים" במקום ה"בקרוב". בדיקות: `supabase/tests/cashflow.sql`.
- CI ב-GitHub Actions (`.github/workflows/ci.yml`): בניית שלוש האפליקציות,
  `astro check` לאתר הציבורי, אימות ש-`supabase/schema.sql` תואם את המיגרציות,
  בדיקות SQL מול service container של `postgres:16`, `deno check` לפונקציות
  הקצה, ו-`npm audit --omit=dev`.
- `.github/pull_request_template.md` ו-`.github/CODEOWNERS`.
- `docs/IMPLEMENTATION_STATUS.md` — האפיון מול הקוד, 20 סעיפים מסווגים.
- `docs/UPGRADES.md` — שדרוגי `astro@7` ו-`react-router-dom@7` הפתוחים.
- `CHANGELOG.md` (המסמך הזה).
- `.env.example` לכל אפליקציה, עם המשתנים שהקוד באמת קורא.
- טבלת בנייה לכל פרויקט Cloudflare (פקודה, תיקיית פלט, משתנים) ב-`docs/DEPLOY.md`,
  וסעיף ענפים/גרסאות/תגיות.
- `npm run build:all`, `check:public`, `deploy:team|candidate|public`.
- `@astrojs/check` ו-`typescript` ל-devDependencies של האתר הציבורי —
  בלעדיהם `npm run check` נפל ל-prompt אינטראקטיבי והאתר לא עבר typecheck מעולם.

### שונה
- `engines.node` מ-`>=20` ל-`>=22`, בהתאם ל-`.node-version` ולדרישת
  `wrangler` ו-`@supabase/supabase-js`.
- `wrangler` אוחד ל-devDependency **אחד בשורש** במקום שלושה (חסך ~242MB
  של workerd/miniflare מכל בנייה ב-Cloudflare).
- `scripts/db-test.sh`: איתור בינאריים של Postgres דרך `pg_config` עם נפילה
  לנתיבי Debian/Homebrew במקום נתיב קשיח; בדיקה ש-`psql` קיים; איסוף קבצי
  הבדיקה ב-glob כך שבדיקה חדשה נכללת אוטומטית; ומצב `DATABASE_URL` מסרב
  לרוץ מול יעד שאינו נראה חד-פעמי אלא עם `--yes-destroy`.
- `scripts/build-schema.sh`: `nullglob` במקום parsing של `ls`, שגיאה ברורה
  על תיקייה ריקה, וקובץ פלט שניתן להעביר כארגומנט.
- התיעוד מיישר קו: הפריסה היא **Cloudflare Workers (Static Assets)** ולא
  Pages, ודומיין הצוות הוא **`hr-app.ort-tech.co.il`** ולא `app.hr` —
  בשני המקרים הקוד הוא שקבע.
- `docs/SPEC.md` מסומן 0.12 (היה 0.8 מול יומן שינויים שהגיע ל-0.12); רשומת 0.1
  הועברה למקומה הכרונולוגי; נוספה הפניה למסמך מצב המימוש.
- README: טבלת המיגרציות המיושנת (0001–0007 מתוך 21) הוחלפה בסיכום והפניה;
  `apps/` כבר לא מסומן "בבנייה"; נוספו סעיפי CI, משתני סביבה, Git ו-`.claude/`.

### הוסר
- `wrangler.toml` מהשורש — שכפל את `apps/team-app/wrangler.toml` והסתמך על
  הערה שמבקשת מבני אדם לסנכרן ידנית. כל פקודת wrangler מקבלת כעת `--config`.
- ה-glob `packages/*` מ-`workspaces` — התיקייה מעולם לא נוצרה, והמודל
  הביטחוני של הריפו ("אין קוד משותף בין הבנדלים") שולל חבילה משותפת.
- מ-`.env.example`: חמישה משתנים שאף קוד אינו קורא (`RESEND_API_KEY`,
  `SMS_PROVIDER_KEY`, `WHATSAPP_TOKEN`, `ANTHROPIC_API_KEY`,
  `PUBLIC_SITE_DEPLOY_HOOK`).

### תוקן
- `.gitignore` חוסם כעת את `.claude/settings.local.json`.

### אבטחה
- 5 פגיעויות ידועות ב-`npm audit --omit=dev` — 1 critical (`astro`: XSS ב-
  `define:vars`, RCE דרך AVIF, SSRF), 1 high (`sharp`/libvips), 2 moderate
  (`react-router`: open redirect), 1 low (`esbuild`). **טרם תוקנו** — התיקון
  היחיד הוא שדרוגי major. ראו `docs/UPGRADES.md`.

---

## [0.12] — 10.9.2026

**נוסף** כרטיס משרה ייעודי: שינוי סטטוס (פתוחה/בהמתנה/אויישה/סגורה/פתיחה
מחדש), רשימת המועמדים שנגשו, לוג שינויי סטטוס עם המשנה והמועד, פרטים מלאים
ושדות מותאמים. תאריכי פתיחה וסגירה מתוחזקים אוטומטית. מיגרציה `0019`.

## [0.11] — 10.9.2026

**נוסף** מנוע שדות מותאמים ללא קוד — הגדרת שדות נוספים לכל סוג ישות (מועמד,
משרה, לקוח, תהליך גיוס) שמופיעים אוטומטית בכרטיס ובטופס העריכה. מיגרציה `0018`.
**תוקן** גלילה אופקית של טבלאות בנייד.

## [0.10] — 10.9.2026

**שונה** טופס ההגשה באתר הפך ניתן לעריכה מהמערכת: תבנית פעילה מסומנת כ"טופס
ההגשה באתר" ושדותיה מוצגים באתר הציבורי בלי פריסה מחדש; התשובות נשמרות
ב-`applications.answers`. **נוסף** תבניות טפסים מוכנות (קליטת עובד, הצהרת
בריאות, אישור פרטים, שאלות הגשה). מיגרציות `0016`–`0017`.

## [0.9] — 10.9.2026

**נוסף** מודול טפסים — מנוע טפסים דינמי גנרי (בונה, שליחה בקישור/מייל/וואטסאפ,
מילוי חיצוני ללא התחברות, מעקב מצב, תיוק ומיפוי לשדות המערכת). **נוסף** מודול
ניהול עובדים ומגייסים (כרטיס עובד, מסמכים, משוב, הזמנה לכניסה דרך פונקציית
הקצה `invite-employee`). מיגרציות `0013`–`0015`.

## [0.8] — 9.9.2026

**נוסף** מודל המידור: שלושה אזורי אמון בשלוש כתובות ובשלוש פריסות נפרדות.
האתר הציבורי נבנה סטטית ואין לו גישה למסד. **תחילת הבנייה בפועל** —
`apps/public-site`, `apps/team-app`, `apps/candidate-app`.

## [0.1] – [0.7] — 9.9.2026

גיבוש האפיון: מטריצת ההרשאות ושישה תפקידים, מנגנון המשימות, כללי
ההתחשבנות (מדרגות שוליות, לוח תשלומים, קיזוז), ומודל הנתונים.
הפירוט המלא ביומן השינויים של [`docs/SPEC.md`](docs/SPEC.md).
