# Default root deployment targets the team app, matching npm run build.
# Keep the name and SPA routing aligned with apps/team-app/wrangler.toml.
name = "hr-team-app"
compatibility_date = "2025-09-01"

[assets]
directory = "./apps/team-app/dist"
not_found_handling = "single-page-application"
